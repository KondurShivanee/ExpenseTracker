apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: expense-tracker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15
          env:
            - name: POSTGRES_DB
              value: expense_db
            - name: POSTGRES_USER
              value: expense_user
            - name: POSTGRES_PASSWORD
              value: expense_pass
          ports:
            - containerPort: 5432
